Problem (unicode1): Understanding Unicode (1 point)
(a) What Unicode character does chr(0) return?

Deliverable: 

Answer:
Will return NULL that is not printable

(b) How does this character’s string representation (__repr__()) differ from its printed representa-
tion?

Deliverable: 

Answer:
print(repr(chr(0))) will print '\x00' 

(c) What happens when this character occurs in text? It may be helpful to play around with the
following in your Python interpreter and see if it matches your expectations:
>>> chr(0)
>>> print(chr(0))
>>> "this is a test" + chr(0) + "string"
>>> print("this is a test" + chr(0) + "string")
Deliverable: 

Answer:
Will not print anything from chr(0)



Problem (unicode2): Unicode Encodings (3 points)
(a) What are some reasons to prefer training our tokenizer on UTF-8 encoded bytes, rather than
UTF-16 or UTF-32? It may be helpful to compare the output of these encodings for various
input strings.
Deliverable: A one-to-two sentence response.

Answer:
UTF-8 most common encoding on the web. Compact enough for a model
Aπ🚀
UTF-8 
41 CF 80 F0 9F 9A 80
Total: 7 bytes.

UTF-16 (Balanced but Bloated)
00 41 03 C0 D8 3D DE 80
Total: 8 bytes.

UTF-32 (Fixed & Massive)
00 00 00 41 00 00 03 C0 00 01 F6 80
Total: 12 bytes.


(b) Consider the following (incorrect) function, which is intended to decode a UTF-8 byte string into
a Unicode string. Why is this function incorrect? Provide an example of an input byte string
that yields incorrect results.
def decode_utf8_bytes_to_str_wrong(bytestring: bytes):
return "".join([bytes([b]).decode("utf-8") for b in bytestring])
>>> decode_utf8_bytes_to_str_wrong("hello".encode("utf-8"))
'hello'
Deliverable: An example input byte string for which decode_utf8_bytes_to_str_wrong pro-
duces incorrect output, with a one-sentence explanation of why the function is incorrect.

Answer:
# The byte string for "é"
input_bytes = b'\xc3\xa9'

# This will raise a UnicodeDecodeError
decode_utf8_bytes_to_str_wrong(input_bytes)

The function is incorrect because it attempts to decode each byte in isolation, 
failing to recognize that UTF-8 is a variable-width encoding where a single 
character (like an emoji or accented letter) is often composed of a specific sequence of multiple bytes.


(c) Give a two byte sequence that does not decode to any Unicode character(s).
Deliverable: An example, with a one-sentence explanation

Answer:
0xC0 0xAF
0xC0 is an invalid start byte because it's "overlong."
0xAF is a valid continuation byte, but because the first byte is forbidden, 
the decoder will throw an error or display a replacement character (like ****).




Problem (train_bpe_tinystories): BPE Training on TinyStories (2 points)
(a) Train a byte-level BPE tokenizer on the TinyStories dataset, using a maximum vocabulary size
of 10,000. Make sure to add the TinyStories <|endoftext|> special token to the vocabulary.
Serialize the resulting vocabulary and merges to disk for further inspection. How many hours
and memory did training take? What is the longest token in the vocabulary? Does it make sense?
Resource requirements: ≤30 minutes (no GPUs), ≤ 30GB RAM
Hint You should be able to get under 2 minutes for BPE training using multiprocessing during
pretokenization and the following two facts:
(a) The <|endoftext|> token delimits documents in the data files.
(b) The <|endoftext|> token is handled as a special case before the BPE merges are applied.
Deliverable: A one-to-two sentence response.

Answer:
% uv run python -m ece405_basics.train_bpe_tinystories
    Learned vocab size: 10000
    Learned merges: 9743

    Vocabulary serialized to ../ece405_basics/bpe_output/vocab.json
    Merges serialized to ../ece405_basics/bpe_output/merges.json

What is the longest token in the vocabulary?

    Longest token length: 15 bytes
    Longest token:  accomplishment

How many hours and memory did training take?

    Training time: 0.0204 hours (73.47 seconds)
    Memory usage: 169.73 MB

Does it make sense?

    Looks fine.


(b) Profile your code. What part of the tokenizer training process takes the most time?
Deliverable: A one-to-two sentence response.

Answer:
% uv run python -m cProfile -o profile.stats -m ece405_basics.train_bpe_tinystories
% uv run python -c "import pstats; p = pstats.Stats('profile.stats'); p.sort_stats('cumulative').print_stats(30)"

Lambda function in max() - 36% of runtime




Problem (train_bpe_expts_owt): BPE Training on OpenWebText (2 points)
(a) Train a byte-level BPE tokenizer on the OpenWebText dataset, using a maximum vocabulary
size of 32,000. Serialize the resulting vocabulary and merges to disk for further inspection. What
is the longest token in the vocabulary? Does it make sense?
Resource requirements: ≤12 hours (no GPUs), ≤ 100GB RAM
Deliverable: A one-to-two sentence response.


% python3 ece405_basics/train_bpe_expts_owt.py  
Learned vocab size: 32000
Learned merges: 31743

Vocabulary serialized to /ece405_basics/bpe_output/vocab_owt.json
Merges serialized to /ece405_basics/bpe_output/merges_owt.json

Longest token length: 64 bytes
Longest token: ÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂÃÂ

Training time: 5.0110 hours (18039.62 seconds)
Memory usage: 10873.41 MB



(b) Compare and contrast the tokenizer that you get training on TinyStories versus OpenWebText.
Deliverable: A one-to-two sentence response.

73.47 seconds vs. 5.0110 hours
vocab size: 10000 vs. 32000
Memory usage: 169.73 MB vs 10873.41 MB


2.7 Experiments
Problem (tokenizer_experiments): Experiments with tokenizers (4 points)
(a) Sample 10 documents from TinyStories and OpenWebText. Using your previously-trained TinyS-
tories and OpenWebText tokenizers (10K and 32K vocabulary size, respectively), encode these
sampled documents into integer IDs. What is each tokenizer’s compression ratio (bytes/token)?
Deliverable: A one-to-two sentence response.

Answer:

python3 ece405_basics/tokenizer_experiments.py
TinyStories tokenizer (10k vocab) on TinyStories sample (10 docs):
  total bytes   = 7402
  total tokens  = 1730
  bytes/token   = 4.2786

OpenWebText tokenizer (32k vocab) on OpenWebText sample (10 docs):
  total bytes   = 34104
  total tokens  = 7552
  bytes/token   = 4.5159

(b) What happens if you tokenize your OpenWebText sample with the TinyStories tokenizer? Com-
pare the compression ratio and/or qualitatively describe what happens.
Deliverable: A one-to-two sentence response.

Answer:

% python3 ece405_basics/tokenizer_experiments.py

Tokenizing the OpenWebText sample with the TinyStories tokenizer 
gives significantly worse compression than the OpenWebText tokenizer: 
about 3.31 bytes/token vs 4.52 bytes/token (so the TinyStories tokenizer needs ~36% more tokens for the same text).

(c) Estimate the throughput of your tokenizer (e.g., in bytes/second). How long would it take to
tokenize the Pile dataset (825GB of text)?
Deliverable: A one-to-two sentence response.

Answer:

% python3 ece405_basics/tokenizer_experiments.py

TinyStories: elapsed encode time 0.000919 s, 8.051 MB/s, 28.46 hours for 825GB
OpenWebText: elapsed encode time 0.003596 s, 9.485 MB/s, 24.16 hours for 825GB

(d) Using your TinyStor'
ies and OpenWebText tokenizers, encode the respectiv
e training and devel-
opment datasets into a sequence of in
teger token IDs. We’ll use this later to train our language
model. We recommend se
rializing the token IDs as a NumPy array of datatype uint16. Why is
uint16 an app
ropriate choice?

Answer:

uint16 is appropriate because token IDs only need to cover the vocabulary size, 
and both of tokenizers are far below 65536 IDs (TinyStories: 10k, OpenWebText: 32k, 
plus special tokens). Using uint16 therefore stores every token ID safely while 
cutting storage roughly  in half versus int32 (and to one-quarter of int64), 
which is important for large encoded train/dev arrays.



3.4.2 Linear Module
Problem (linear): Implementing the linear module (1 point)

ece405_basics/linear.py

uv run pytest -k test_linear


3.4.3 Embedding Module
Problem (embedding): Implement the embedding module (1 point)

ece405_basics/embedding.py

uv run pytest -k test_embedding


3.5.1 Root Mean Square Layer Normalization
Problem (rmsnorm): Root Mean Square Layer Normalization (1 point)

ece405_basics/rmsnorm.py

uv run pytest -k test_rmsnorm

3.5.2 Position-Wise Feed-Forward Network
Problem (positionwise_feedforward): Implement the position-wise feed-forward network
(2 points)

ece405_basics/positionwise_feedforward.py

uv run pytest -k test_swiglu


Problem (rope): Implement RoPE (2 points)

ece405_basics/rope.py

uv run pytest -k test_rope



3.5.4 Scaled Dot-Product Attention
Problem (scaled_dot_product_attention): Implement scaled dot-product attention
(5 points)

ece405_basics/scaled_dot_product_attention.py

uv run pytest -k test_scaled_dot_product_attention

3.5.5 Causal Multi-Head Self-Attention
Problem (multihead_self_attention): Implement causal multi-head self-attention (5
points)

ece405_basics/multihead_self_attention.py

uv run pytest -k test_multihead_self_attention




3.6 The Full Transformer LM
Problem (transformer_block): Implement the Transformer block (3 points)

ece405_basics/transformer_block.py

uv run pytest -k test_transformer_block


Problem (transformer_lm): Implementing the Transformer LM (3 points)

ece405_basics/transformer_lm.py

uv run pytest -k test_transformer_lm


Problem (transformer_accounting): Transformer LM resource accounting (5 points)

ece405_basics/transformer_accounting.py

uv run pytest -k test_transformer_accounting

Problem (transformer_accounting): Transformer LM resource accounting (5 points)
(a) Consider GPT-2 XL, which has the following configuration:
vocab_size : 50,257
context_length : 1,024
num_layers : 48
d_model : 1,600
num_heads : 25
d_ff : 6,400
Suppose we constructed our model using this configuration. How many trainable parameters
would our model have? Assuming each parameter is represented using single-precision floating
point, how much memory is required to just load this model?
Deliverable: A one-to-two sentence response.

Answer: python3 -m ece405_basics.transformer_accounting
Parameters: 2,127,057,600
FP32 memory (bytes): 8,508,230,400
FP32 memory (GiB): 7.9239
FP32 memory (GB): 8.5082

(b) Identify the matrix multiplies required to complete a forward pass of our GPT-2 XL-shaped
model. How many FLOPs do these matrix multiplies require in total? Assume that our input
sequence has context_length tokens.
Deliverable: A list of matrix multiplies (with descriptions), and the total number of FLOPs
required.

Answer: python3 -m ece405_basics.transformer_accounting
Forward matmul FLOPs breakdown:
- Q, K, V projections: (B*T x d) @ (d x d) | count=144 | FLOPs=754,974,720,000
- Attention output projection: (B*T x d) @ (d x d) | count=48 | FLOPs=251,658,240,000
- Attention scores QK^T per head: (B*T x d_h) @ (d_h x T) | count=1,200 | FLOPs=161,061,273,600
- Attention-weighted values per head: (B*T x T) @ (T x d_h) | count=1,200 | FLOPs=161,061,273,600
- SwiGLU w1: (B*T x d) @ (d x d_ff) | count=48 | FLOPs=1,006,632,960,000
- SwiGLU w3: (B*T x d) @ (d x d_ff) | count=48 | FLOPs=1,006,632,960,000
- SwiGLU w2: (B*T x d_ff) @ (d_ff x d) | count=48 | FLOPs=1,006,632,960,000
- Final LM head: (B*T x d) @ (d x vocab_size) | count=1 | FLOPs=164,682,137,600
Total forward matmul FLOPs: 4,513,336,524,800
Matches expected total: True

(c) Based on your analysis above, which parts of the model require the most FLOPs?
Deliverable: A one-to-two sentence response.

Answer:
The SwiGLU feed-forward layers

(d) Repeat your analysis with GPT-2 small (12 layers, 768 d_model, 12 heads), GPT-2 medium (24
layers, 1024 d_model, 16 heads), and GPT-2 large (36 layers, 1280 d_model, 20 heads). As the
model size increases, which parts of the Transformer LM take up proportionally more or less of
the total FLOPs?
Deliverable: For each model, provide a breakdown of model components and its associated
FLOPs (as a proportion of the total FLOPs required for a forward pass). In addition, provide a
one-to-two sentence description of how varying the model size changes the proportional FLOPs
of each component.

Answer: python3 -m ece405_basics.transformer_accounting
GPT-2 small/medium/large FLOPs proportions:
gpt2-small total FLOPs: 349,630,365,696
- Q, K, V projections: (B*T x d) @ (d x d) | FLOPs=43,486,543,872 | proportion=12.4379%
- Attention output projection: (B*T x d) @ (d x d) | FLOPs=14,495,514,624 | proportion=4.1460%
- Attention scores QK^T per head: (B*T x d_h) @ (d_h x T) | FLOPs=19,327,352,832 | proportion=5.5279%
- Attention-weighted values per head: (B*T x T) @ (T x d_h) | FLOPs=19,327,352,832 | proportion=5.5279%
- SwiGLU w1: (B*T x d) @ (d x d_ff) | FLOPs=57,982,058,496 | proportion=16.5838%
- SwiGLU w3: (B*T x d) @ (d x d_ff) | FLOPs=57,982,058,496 | proportion=16.5838%
- SwiGLU w2: (B*T x d_ff) @ (d_ff x d) | FLOPs=57,982,058,496 | proportion=16.5838%
- Final LM head: (B*T x d) @ (d x vocab_size) | FLOPs=79,047,426,048 | proportion=22.6089%
gpt2-medium total FLOPs: 1,033,109,504,000
- Q, K, V projections: (B*T x d) @ (d x d) | FLOPs=154,618,822,656 | proportion=14.9664%
- Attention output projection: (B*T x d) @ (d x d) | FLOPs=51,539,607,552 | proportion=4.9888%
- Attention scores QK^T per head: (B*T x d_h) @ (d_h x T) | FLOPs=51,539,607,552 | proportion=4.9888%
- Attention-weighted values per head: (B*T x T) @ (T x d_h) | FLOPs=51,539,607,552 | proportion=4.9888%
- SwiGLU w1: (B*T x d) @ (d x d_ff) | FLOPs=206,158,430,208 | proportion=19.9551%
- SwiGLU w3: (B*T x d) @ (d x d_ff) | FLOPs=206,158,430,208 | proportion=19.9551%
- SwiGLU w2: (B*T x d_ff) @ (d_ff x d) | FLOPs=206,158,430,208 | proportion=19.9551%
- Final LM head: (B*T x d) @ (d x vocab_size) | FLOPs=105,396,568,064 | proportion=10.2019%
gpt2-large total FLOPs: 2,257,754,521,600
- Q, K, V projections: (B*T x d) @ (d x d) | FLOPs=362,387,865,600 | proportion=16.0508%
- Attention output projection: (B*T x d) @ (d x d) | FLOPs=120,795,955,200 | proportion=5.3503%
- Attention scores QK^T per head: (B*T x d_h) @ (d_h x T) | FLOPs=96,636,764,160 | proportion=4.2802%
- Attention-weighted values per head: (B*T x T) @ (T x d_h) | FLOPs=96,636,764,160 | proportion=4.2802%
- SwiGLU w1: (B*T x d) @ (d x d_ff) | FLOPs=483,183,820,800 | proportion=21.4011%
- SwiGLU w3: (B*T x d) @ (d x d_ff) | FLOPs=483,183,820,800 | proportion=21.4011%
- SwiGLU w2: (B*T x d_ff) @ (d_ff x d) | FLOPs=483,183,820,800 | proportion=21.4011%
- Final LM head: (B*T x d) @ (d x vocab_size) | FLOPs=131,745,710,080 | proportion=5.8353%

(e) Take GPT-2 XL and increase the context length to 16,384. How does the total FLOPs for one
forward pass change? How do the relative contribution of FLOPs of the model components
change?
Deliverable: A one-to-two sentence response.

Answer: python3 -m ece405_basics.transformer_accounting
GPT-2 XL context scaling (1024 -> 16384):
Base total FLOPs: 4,513,336,524,800
New total FLOPs: 149,522,795,724,800
Total FLOPs multiplier: 33.1291x
- Q, K, V projections: (B*T x d) @ (d x d) | base=16.7276% | new=8.0788%
- Attention output projection: (B*T x d) @ (d x d) | base=5.5759% | new=2.6929%
- Attention scores QK^T per head: (B*T x d_h) @ (d_h x T) | base=3.5686% | new=27.5755%
- Attention-weighted values per head: (B*T x T) @ (T x d_h) | base=3.5686% | new=27.5755%
- SwiGLU w1: (B*T x d) @ (d x d_ff) | base=22.3035% | new=10.7717%
- SwiGLU w3: (B*T x d) @ (d x d_ff) | base=22.3035% | new=10.7717%
- SwiGLU w2: (B*T x d_ff) @ (d_ff x d) | base=22.3035% | new=10.7717%
- Final LM head: (B*T x d) @ (d x vocab_size) | base=3.6488% | new=1.7622%



4. Training a Transformer LM

4.1 Cross-entropy loss
Problem (cross_entropy): Implement Cross entropy

ece405_basics/cross_entropy.py

uv run pytest -k test_cross_entropy


4.2 The SGD Optimizer
4.2.1 Implementing SGD in PyTorch

Problem (learning_rate_tuning): Tuning the learning rate (1 point)
Deliverable: A one-to-two sentence response with the behaviors observed.

Answer: ece405_basics/learning_rate_tuning.py
A larger learning rate can accelerate convergence up to a point, 
but too large a value becomes unstable.


4.3 AdamW
Problem (adamw): Implement AdamW (2 points)

ece405_basics/adamw.py

uv run pytest -k test_adamw


Problem (adamwAccounting): Resource accounting for training with AdamW (2 points)

(a) How much peak memory does running AdamW require? Decompose your answer based on the
memory usage of the parameters, activations, gradients, and optimizer state. Express your answer
in terms of the batch_size and the model hyperparameters (vocab_size, context_length,
num_layers, d_model, num_heads). Assume d_ff = 4 ×d_model.
For simplicity, when calculating memory usage of activations, consider only the following compo-
nents:
• Transformer block
– RMSNorm(s)
– Multi-head self-attention sublayer: QKV projections, Q⊤K matrix multiply, softmax,
weighted sum of values, output projection.
– Position-wise feed-forward: W1 matrix multiply, SiLU, W2 matrix multiply
• final RMSNorm
• output embedding
• cross-entropy on logits
Deliverable: An algebraic expression for each of parameters, activations, gradients, and opti-
mizer state, as well as the total.

Answer: python3 -m ece405_basics.adamwAccounting

Algebraic expressions:
- parameters_elements: P = 2Vd + L(4d^2 + 2dd_ff + 2d) + d, with d_ff = 4d; equivalently P = 2Vd + 12Ld^2 + (2L + 1)d
- activations_elements: A = L[2BTd + 3BTd + BHT^2 + BHT^2 + BTd + BTd + BTd_ff + BTd_ff + BTd] + BTd + BTV + BTV, with d_ff = 4d; equivalently A = L(16BTd + 2BHT^2) + BTd + 2BTV
- gradients_elements: G = P
- optimizer_state_elements: O = 2P (AdamW moments m and v)
- total_peak_bytes: M_peak = 4(P + A + G + O) = 4(A + 4P) bytes (float32, 4 bytes per element)



(b) Instantiate your answer for a GPT-2 XL-shaped model to get an expression that only depends on
the batch_size. What is the maximum batch size you can use and still fit within 80GB memory?
Deliverable: An expression that looks like a ·batch_size + b for numerical values a, b, and a
number representing the maximum batch size.

Answer: python3 -m ece405_basics.adamwAccounting

GPT-2 XL instantiation (80 GB limit):
- M_peak(B) = 15,517,753,344 * B + 26,168,601,600 bytes
- M_peak(B) = 15.517753344 * B + 26.168601600 GB
- Max batch size under 80 GB: 3


(c) How many FLOPs does running one step of AdamW take?
Deliverable: An algebraic expression, with a brief justification.

Answer: python3 -m ece405_basics.adamwAccounting

F_AdamW_step = 16P FLOPs, where P is the number of trainable parameters.
Each parameter element performs about 16 floating-point ops in AdamW: 
m update (3), v update (4), bias corrections (2), sqrt+eps (2), 
normalization divide (1), and decoupled weight-decay + parameter update (4). 
Ignoring O(1) scalar terms, total cost is linear in parameter count.


(d) Model FLOPs utilization (MFU) is defined as the ratio of observed throughput (tokens per second)
relative to the hardware’s theoretical peak FLOP throughput [Chowdhery et al., 2022]. An
NVIDIA A100 GPU has a theoretical peak of 19.5 teraFLOP/s for float32 operations. Assuming
you are able to get 50% MFU, how long would it take to train a GPT-2 XL for 400K steps and a
batch size of 1024 on a single A100? Following Kaplan et al. [2020] and Hoffmann et al. [2022],
assume that the backward pass has twice the FLOPs of the forward pass.
Deliverable: The number of days training would take, with a brief justification.

Answer: python3 -m ece405_basics.adamwAccounting
AdamW one-step FLOPs:
- F_AdamW_step = 16P FLOPs, where P is the number of trainable parameters (ignoring O(1) scalar ops). Per parameter element: m update (3) + v update (4) + bias corrections m_hat/v_hat (2) + sqrt+eps (2) + normalization divide (1) + decoupled weight decay and parameter update (4) = 16 FLOPs.
- GPT-2 XL instantiation: F_AdamW_step = 26,168,601,600 FLOPs

MFU training-time estimate (single A100, GPT-2 XL):
- days = [steps * batch_size * F_fwd(B=1) * (1 + r_bw)] / [MFU * F_peak] / 86400, where r_bw is backward-to-forward FLOPs ratio (r_bw = 2 here).
- Estimated training time for 400k steps, batch size 1024: 6583.56 days



4.4 Learning rate scheduling

Problem (learning_rate_schedule): Implement cosine learning rate schedule with
warmup

ece405_basics/learning_rate_schedule.py

uv run pytest -k test_get_lr_cosine_schedule


4.5 Gradient clipping
Problem (gradient_clipping): Implement gradient clipping (1 point)

ece405_basics/gradient_clipping.py

uv run pytest -k test_gradient_clipping



5. Training loop
5.1 Data Loader

Problem (data_loading): Implement data loading (2 points)

ece405_basics/data_loading.py

uv run pytest -k test_get_batch


5.2 Checkpointing
Problem (checkpointing): Implement model checkpointing (1 point)

ece405_basics/checkpointing.py

uv run pytest -k test_checkpointing


5.3 Training loop
Problem (training_together): Put it together (4 points)

ece405_basics/training_together.py


6. Generating text
Problem (decoding): Decoding (3 points)

ece405_basics/decoding.py



7. Experiments
7.1 How to Run Experiments and Deliverables
Problem (experiment_log): Experiment logging (3 points)

ece405_basics/experiment_log.py

7.2 TinyStories
Problem (learning_rate): Tune the learning rate (3 points) (4 H100 hrs)

The learning rate is one of the most important hyperparameters to tune. Taking the base model
you’ve trained, answer the following questions:
(a) Perform a hyperparameter sweep over the learning rates and report the final losses (or note
divergence if the optimizer diverges).
Deliverable: Learning curves associated with multiple learning rates. Explain your hyperpa-
rameter search strategy.
Deliverable: A model with validation loss (per-token) on TinyStories of at most 1.45

Answer:

ece405_basics/learning_rate.py

Custom LR sweep test: 

uv run python -m ece405_basics.learning_rate  --device mps --lrs 1e-4,2e-4,3e-4,5e-4,8e-4,1e-3,0.01

## Search strategy
Coarse-to-fine around the stability edge: run increasing learning rates to find divergence, 
then select the largest stable learning rate with best validation loss.

## Sweep settings
- learning_rates: 1.00e-04, 2.00e-04, 3.00e-04, 5.00e-04, 8.00e-04, 1.00e-03
0.0001 - 0.1
- total_tokens: 40000000
- context_length: 256
- batch_size: 64
- target_val_loss: 2.0

## Results

| lr_max | status | best_val_loss | best_val_step | run_dir |
| 1.00e-04 | completed | 6.3361 | 100 | experiments/lr-sweep-1e-04-20260223-111835 |
| 2.00e-04 | completed | 6.2628 | 100 | experiments/lr-sweep-2e-04-20260223-121323 |
| 3.00e-04 | completed | 6.2591 | 100 | experiments/lr-sweep-3e-04-20260223-135645 |
| 5.00e-04 | completed | 6.2635 | 100 | experiments/lr-sweep-5e-04-20260223-142607 |
| 8.00e-04 | completed | 6.3013 | 100 | experiments/lr-sweep-8e-04-20260223-145528 |
| 1.00e-03 | completed | 6.2780 | 100 | experiments/lr-sweep-1e-03-20260223-152452 |

| lr | status | final val loss | best val loss | best step | run dir |
| 0.0001 | ok | 3.379166 | 3.379166 | 4882 | experiments/lr-sweep-1e-04-001-20260224-060803 |
| 0.0002 | ok | 3.026007 | 3.026007 | 4882 | experiments/lr-sweep-2e-04-001-20260224-063658 |
| 0.0003 | ok | 2.876508 | 2.876508 | 4882 | experiments/lr-sweep-3e-04-002-20260224-064223 |
| 0.0005 | ok | 2.731657 | 2.731657 | 4882 | experiments/lr-sweep-5e-04-003-20260224-064754 |
| 0.0008 | ok | 2.617306 | 2.617306 | 4882 | experiments/lr-sweep-8e-04-004-20260224-065317 |
| 0.001 | ok | 2.570520 | 2.570520 | 4882 | experiments/lr-sweep-1e-03-005-20260224-065846 |
| 0.0012 | ok | 2.538825 | 2.538825 | 4882 | experiments/lr-sweep-1e-03-001-20260224-071210 |
| 0.0015 | ok | 2.510689 | 2.510689 | 4882 | experiments/lr-sweep-2e-03-002-20260224-071745 |
| 0.002 | ok | 2.468856 | 2.468856 | 4882 | experiments/lr-sweep-2e-03-003-20260224-072320 |
| 0.003 | ok | 2.433179 | 2.433179 | 4882 | experiments/lr-sweep-3e-03-004-20260224-072845 |
| 0.01 | ok | 2.400309 | 2.400309 | 4882 | experiments/lr-sweep-1e-02-001-20260224-075323 |
| 0.1 | ok | 2.551666 | 2.551666 | 4750 | experiments/lr-sweep-1e-01-001-20260224-075923 |


## Analysis
- Best stable run: lr=0.01 with best_val_loss=2.400309 at step 4882.
- Target not met: best validation loss is above 2.00.



(b) Folk wisdom is that the best learning rate is “at the edge of stability.” Investigate how the point
at which learning rates diverge is related to your best learning rate.
Deliverable: Learning curves of increasing learning rate which include at least one divergent
run and an analysis of how this relates to convergence rates.

Answer:

Commands used:
- uv run python -m ece405_basics.learning_rate --device mps --token-budget 2097152 --batch-size 32 --context-length 256 --eval-interval 16 --log-interval 8 --checkpoint-interval 64 --eval-batches 5 --lrs 0.01,0.03,0.1,0.3,1.0
- uv run python -m ece405_basics.learning_rate --device mps --token-budget 524288 --batch-size 32 --context-length 256 --eval-interval 8 --log-interval 4 --checkpoint-interval 16 --eval-batches 4 --lrs 3.0,10.0,30.0
- uv run python -m ece405_basics.learning_rate --device mps --token-budget 262144 --batch-size 32 --context-length 256 --eval-interval 8 --log-interval 4 --checkpoint-interval 16 --eval-batches 4 --lrs 100.0,300.0

Search strategy:
Increase LR monotonically to bracket the stability edge, then compare both convergence speed and endpoint validation loss.

Learning curves (validation loss at common checkpoints; includes divergent run):

| lr | status | val@8 | val@16 | val@32 | val@64 | val@128 | val@192 | val@256 | run_dir |
| 0.01 | ok | - | 6.009 | 5.536 | 4.930 | 4.140 | 3.807 | 3.619 | experiments/lr-sweep-1e-02-001-20260224-090626 |
| 0.1 | ok | - | 6.019 | 5.870 | 5.683 | 4.907 | 4.406 | 4.033 | experiments/lr-sweep-1e-01-003-20260224-090718 |
| 1.0 | ok | - | 14.686 | 7.518 | 6.080 | 5.768 | 5.301 | 5.998 | experiments/lr-sweep-1e00-005-20260224-090811 |
| 30.0 | ok (unstable) | 9.210 | 9.210 | 9.210 | 17.862 | - | - | - | experiments/lr-sweep-3e01-003-20260224-091237 |
| 300.0 | diverged | nan | nan | nan | - | - | - | - | experiments/lr-sweep-3e02-002-20260224-091303 |

Analysis (edge of stability vs. best LR and convergence rates):
- Best learning rate in this sweep is lr=0.01 (best/final val loss 3.619 at step 256).
- First formally divergent run appears at lr=300.0 (non-finite val loss from the first eval, step 8).
- The hard divergence threshold is therefore much higher than the best LR (roughly 30000x larger), so in this setup the best LR is not numerically right at the divergence boundary.
- Practical instability starts earlier: lr=30.0 shows severe loss spikes (e.g., val loss jumping to ~2.56e7 before partial recovery), and lr>=1.0 gives noisy curves and weaker final validation loss.
- Convergence-rate comparison from stable runs (approx val-loss drop per 100 steps):
  - lr=0.01: ~1.0 / 100 steps (fastest sustained improvement among stable runs)
  - lr=0.1: ~0.83 / 100 steps
  - lr=1.0: rapid early drop but poor late-stage stability and worse endpoint
- Conclusion: this model/schedule (with clipping and cosine decay) favors an LR below the instability region; “edge of stability” is better interpreted as the onset of noisy/explosive curves, not only first NaN divergence.





Problem (batch_size_experiment): Batch size variations (1 point) (2 H100 hrs)
Vary your batch size all the way from 1 to the GPU memory limit. Try at least a few batch sizes
in between, including typical sizes like 64 and 128.
Deliverable: Learning curves for runs with different batch sizes. The learning rates should be
optimized again if necessary.
Deliverable: A few sentences discussing of your findings on batch sizes and their impacts on
training.

Answer: 
uv run python -m ece405_basics.batch_size_experiment --device mps --batch-sizes 1,8,32,64,128,256

uv run python -m ece405_basics.batch_size_experiment \
  --device mps \
  --batch-sizes 64 \
  --use-wandb \
  --wandb-project ece405-basics

batch size 

Larger batch sizes (64-256) produced more stable training curves with smoother validation loss trajectories, 
as the gradient estimates become less noisy with more samples per update.


Problem (generate): Generate text (1 point)

Answer:
uv run python -m ece405_basics.generate --checkpoint-path experiments_tmp/ckpt_lr_1e-02_001.pt --config-path experiments/lr-sweep-1e-02-001-20260224-104646/config.json --tokenizer-vocab ece405_basics/bpe_output/vocab.json --tokenizer-merges ece405_basics/bpe_output/merges.json --prompt "Once upon a time" --max-new-tokens 50 --temperature 0.9 --top-p 0.95 --device auto --output-path experiments/generation_report_latest.txt

Generated text (50 tokens):
"Once upon a timeilt Mrs rose monster explorers mom recorded worms spit intentlyedy thoughts Quick see digs applauded Samantha cartoonspping burns natural Jill squawushuUnfortunately relieved coop disappointed herb firefighter notice bushyitedcited ill letting behind earned bowed belongs Em's emergency sec messages dreaming powerful requestune"

The model generates mostly English-like tokens but lacks coherence, which is expected given the small model size (d_model=64, 2 layers) and limited training. The output shows the model learned basic word patterns but struggles with semantic continuity.



7.3 Ablations and architecture modification

Ablation 1: layer normalization
Problem (layer_norm_ablation): Remove RMSNorm and train (1 point) (1 H100 hr)

uv run python -m ece405_basics.layer_norm_ablation --device mps

Problem (pre_norm_ablation): Implement post-norm and train (1 point) (1 H100 hr)

Smoke-test:
uv run python -m ece405_basics.pre_norm_ablation --device cpu --token-budget 32768 --batch-size 16 --context-length 64 --eval-interval 8 --log-interval 4 --checkpoint-interval 8 --eval-batches 2


Ablation 2: position embeddings
Problem (no_pos_emb): Implement NoPE (1 point) (1 H100 hr)
uv run -m ece405_basics.no_pos_emb --token-budget 33000000 --device auto


Ablation 3: SwiGLU vs. SiLU
Problem (swiglu_ablation): SwiGLU vs. SiLU (1 point) (1 H100 hr)

uv run python -m ece405_basics.swiglu_ablation --help



